_build/
_build
_deps*/
_deps
cmd/juju/juju
cmd/jujud/jujud
cmd/builddb/builddb
cmd/charmd/charmd
cmd/charmload/charmload
tests/test-*
tags
!tags/
TAGS
!TAGS/
parts/
prime/
stage/
vendor/
*.snap
.emacs.desktop
.emacs.desktop.lock
*.test
*.test.exe
*.sw[nop]
.idea/
*.pyc
.vscode/
juju-backup-*.tar.gz
.editorconfig
scripts/mgo-run-txn/mgo-run-txn
tests/tmp.*
*.out
*.charm
*.bundle
*.log
dqlite-deps.tar.bz2
.sphinx
.envrc
.direnv/
.codex

# Spec Kit (https://github.com/github/spec-kit) tooling scaffolding.
# `.specify/memory/` is committed — it holds the ratified constitution
# and other durable governance artifacts. The rest of `.specify/` is
# regenerated by `specify init . --here --force` and is not tracked.
.specify/*
!.specify/memory/

# Claude Code project state (skills, settings) — auto-generated by
# spec-kit's Claude integration on init.
.claude/
